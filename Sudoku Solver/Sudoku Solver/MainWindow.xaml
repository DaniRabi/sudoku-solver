<Window x:Class="Sudoku_Solver.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Sudoku_Solver"
        mc:Ignorable="d"
        Title="Sudoku Solver" Height="350" Width="525">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="100"/>
        </Grid.ColumnDefinitions>
        <Border BorderThickness="2" BorderBrush="Black">
            <Grid Name="board" Grid.Column="0"/>
        </Border>
        <StackPanel Grid.Column="1">
            <Button Content="Invisible Button" FontSize="10" Opacity="0"/>
            <Button Content="Solve" FontSize="15" Click="Solve_Click"/>
            <Button Content="Invisible Button" FontSize="10" Opacity="0"/>
            <Button Content="Reset" FontSize="15" Click="Reset_Click"/>
        </StackPanel>
    </Grid>
</Window>
